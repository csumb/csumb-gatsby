import React from 'react'
import { AlertEmpty } from 'components/alert'

const CourseBlock = () => (
  <AlertEmpty>
    All course listings are now available in{' '}
    <a href="/catalog">the university catalog</a>.
  </AlertEmpty>
)

export default CourseBlock
