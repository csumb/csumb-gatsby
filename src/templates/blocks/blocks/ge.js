import React from 'react'

const BlockGe = ({ type, code }) => (
  <>
    <span className="content-type-ge--type">{type}</span>
    <span className="content-type-ge--code">{code}</span>
  </>
)

export default BlockGe
